# Only what the page renders ships, plus the local check stack. Allowlist + reasoning: the `mathewmusango` skill.

__pycache__/
*.py[cod]
.DS_Store

# Local-only; the page renders without them. `.github/`, `LICENSE` and `scripts/checks/` are kept on purpose.
.preview/
data/
scripts/*
!scripts/checks/
