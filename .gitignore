# Only what the page renders ships. Allowlist + reasoning: the `mathewmusango` skill.

__pycache__/
*.py[cod]
.DS_Store

# Local-only; the page renders without them. `.github/` and `LICENSE` are kept on purpose.
.preview/
TODO.md
data/
scripts/
assets/svg/brands/README.md
